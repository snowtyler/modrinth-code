# Windows has stack overflows when calling from Tauri, so we increase the default stack size used by the compiler
[target.x86_64-pc-windows-msvc]
linker = "rust-lld.exe"
rustflags = [
    "-C", "link-arg=/STACK:16777220",
]

[target."cfg(windows)"]
rustflags = [
    "-C", "link-arg=/STACK:16777220",
]
